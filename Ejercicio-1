#include <iostream>
using namespace std;

int x=0;

main (){
    cout<<x<<endl;
}

/*
    Responda a las siguientes preguntas:
    1.  ¿Qué pasa si escribimos el tipo de dato int en mayúsculas INT?
        INT x=0;

        Debido a que no es la escritura correcta para asignar algún tipo de datos admitidos por C++, no compilará y dará un error de Sintaxis.

    2.  ¿Qué pasa si la variable x se inicializa con 8.3?
        int x=8.3;

        Ya que se le asigna un tipo de dato "int" este solo toma el número entero, por lo que se imprimirá en la consola "8", eso con cualquier número que se coloque en la inicialización de la variable, siempre tomará la parte entera del número. Por ejemplo, si se inicializa con "10.99" aunque este, por definición podría aproximar al número, con "int" solamente se toma la parte entera, por lo que imprime "10".

    3.  ¿Qué pasa si se hace una asignación en el contexto global?
        int x=0;
        x=5;

        En este caso, al declarar o asignar una variable en el contexto globlal, se debe de asignar que tipo de datos es, por lo provoca un error al no asignar un tipo de datos a la variable. Por ende, no compilaría el programa.

    4.  ¿Qué pasa si declaramos 2 variables con diferentes tipos con el mismo nombre?
        int x=0;
        double x=0;

        Se crea un conflicto de declaración de variable, ya que esta ya se había declarado con anterioridad, por lo que no compilaría el programa.

    5.  Tome una captura de pantalla y señale cual es el contexto global y cual es el contexto privado del main

*/
